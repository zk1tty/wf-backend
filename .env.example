# We support all langchain models, openai only for demo purposes
OPENAI_API_KEY=
# replace LangChain LLM
BROWSER_USE_MODEL='gpt-4o'
# Supabase Configuration
SUPABASE_URL
SUPABASE_SERVICE_ROLE_KEY
SUPABASE_JWT_SECRET
STORAGE_STATE_JSON_B64
COOKIE_KID # optional
COOKIE_PUBLIC_KEY_PEM
COOKIE_PRIVATE_KEY_PEM
FEATURE_USE_COOKIES=true
CORS_ALLOWED_EXTENSIONS="chrome-extension://<id>"
COOKIE_VERIFY_TTL_HOURS=24

# If set, Pub/Sub is enabled; if not, it runs single-worker mode
REDIS_URL
WEB_CONCURRENCY=2